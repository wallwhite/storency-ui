import React from 'react';
import { DocsLayout } from '@/lib/layouts';

const GuidesRoot = (): JSX.Element => (
    <DocsLayout title="Documentation overview" description="overview">
        GuidesRoot
    </DocsLayout>
);

export default GuidesRoot;
